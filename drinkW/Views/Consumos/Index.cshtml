@model IEnumerable<drinkW.Models.Consumo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataConsumo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipienteConsumo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TamanhoConsumo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataConsumo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RecipienteConsumo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TamanhoConsumo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
